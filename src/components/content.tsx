import React, { useEffect, useContext, useState } from 'react';
import { Toast } from 'antd-mobile';

import './content.pcss';

export default () => {
  useEffect(() => {
  }, []);

  return (
    <article className="coral-content">
      11111
    </article>
  )
}
